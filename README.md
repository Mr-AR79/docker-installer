# Docker Engine Installer for Ubuntu

Install **Docker Engine** on Ubuntu using the **official Docker repository** with a single command.

This script removes old Docker versions, installs the latest stable Docker Engine, enables the service, and configures non-root Docker usage.

---

## ✅ Supported Operating Systems

- Ubuntu 20.04 (Focal)
- Ubuntu 22.04 (Jammy)
- Ubuntu 24.04 (Noble)
- Ubuntu Server / VPS / Cloud instances

---

## 🚀 One‑Line Installation
```bash
curl -fsSL https://raw.githubusercontent.com/USERNAME/REPO/main/install_docker.sh | sudo bash
